/** Session storage key shared by the background worker and content scripts. */
export const HUD_WALK_SESSION_KEY = 'palette:hudWalk';

/** postMessage / CustomEvent type iframes use to dismiss the top-frame HUD. */
export const HUD_DISMISS_MESSAGE = 'palette:dismiss-hud';
